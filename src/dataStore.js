// Generated by CoffeeScript 1.8.0
(function() {
  var defaultRoot, fs, pathToFilename, readFile;

  fs = require('fs');

  defaultRoot = './data';

  pathToFilename = function(path) {
    var filename;
    return filename = defaultRoot + path + '.json';
  };

  readFile = function(file) {
    var contents;
    console.log("apiData readFile()", file);
    contents = fs.readFileSync(file, {
      encoding: 'utf8'
    });
    return JSON.parse(contents);
  };

  module.exports = {
    GET: function() {
      return readFile(pathToFilename(arguments[0]));
    },
    POST: function() {
      return console.log("apiData.POST not yet supported", arguments);
    },
    PUT: function() {
      return console.log("apiData.PUT not yet supported", arguments);
    },
    DELETE: function() {
      return console.log("apiData.DELETE not yet supported", arguments);
    }
  };

}).call(this);
